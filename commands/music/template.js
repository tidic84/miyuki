const { MessageEmbed } = require("discord.js");

module.exports.run = (client, message, args) => {
 
};

module.exports.help = {
  name: "template",
  aliases: ["template"],
  category: "music",
  description: "template",
  cooldown: 1,
  usage: "",
  isUserAdmin: false,
  permissions: false,
  args: false,
};
